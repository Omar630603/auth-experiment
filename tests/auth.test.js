// tests using jest and supertest
// test on the auth routes
// test on the auth controller
// test on the auth service
// test on the auth model
// test on the auth helpers (jsonwebtoken, errorhandler)
